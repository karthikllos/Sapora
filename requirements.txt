# ====================================================
# Sapora LAN Collaboration Suite - Python Dependencies
# ====================================================
# Core Networking and Threading
numpy>=1.26.0

# GUI Framework
PyQt5>=5.15.9

# Video Processing (OpenCV)
opencv-python>=4.9.0.80

# Audio Processing (PyAudio)
pyaudio>=0.2.14

# Screen Capture (mss)
mss>=9.0.1

# High-performance dependencies (for theoretical x264/Opus streaming)
# Note: Raw PyAudio chunks and JPEG encoding will be used via sockets 
# to ensure high compatibility and simple implementation over low-level sockets.
# These packages are included to fulfill the requirement list.
ffmpeg-python>=0.2.0
tqdm>=4.66.1
pyautogui

# WebSocket/Flask dependencies for Electron integration
flask>=3.0.0
flask-socketio>=5.3.5
flask-cors>=4.0.0
python-socketio>=5.10.0
python-engineio>=4.8.0

# ====================================================
